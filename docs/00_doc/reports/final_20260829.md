# Final report — SPLEX GPU campaign on 8× B200 — 2026-08-28 20:25 → 2026-08-29 14:00 UTC — ≈ 65 GPU-h of 1,000

1. exp0 PASS (`49bcd8a`): vLLM 0.28.0 on 8× B200 (SM 10.0), backend FLASHINFER_MLA_SPARSE + TRTLLM fp8 MoE; selection hooks (KV top-k + MoE) and ds4-schema adapter verified; `node.json`.
2. exp1 PASS (`db02462`): DeepSeek-V3.2, 135 runs 8K–128K (bf + 2K-step ld). Adjacent top-2048 overlap **0.843/0.790/0.751/0.741/0.723**, lift over random **2.6/5.3/10.2/21.9/41.5×**, recency baseline 0.49→0.31, hot-set A@99 **65.0/44.7/30.3/16.7/9.7 %**, MEASURED_TOP10 0.30/0.57/0.79/0.94/0.98, MoE adj overlap 0.25 (8×, context-independent); retention at lag 1024 (2K decodes) 0.43→0.34. `docs/gpu_sweep/` + 13 GB sharded raw traces.
3. exp2 PASS (`a7fee27`): GLM-5.2 (all-layer / computing-layer variants) and GLM-5 on the same ladder: overlap 0.846→0.702 / 0.847→0.692 / 0.849→0.715, lift 2.6→40×, A@99 62→11–12 %, MoE 0.29 (9×) — all within ±0.02 overlap / ±2 pt of V3.2. `docs/glm_sweep/` (+21 GB shards), `side_by_side.png`.
4. exp3 tier 1 PASS (this commit): perplexity protocol (user decision; bit-exact reruns impossible — 11 probes). Long-book PPL after re-indexing a 32K/64K/128K prefix and WikiText-2: **77/78 mode rows within the identical-rerun noise floor** for impl A and impl B on all three models (single exception = one high-variance document at one seed, cleared by two more seeds); RULER answers unchanged (16/20, 18/20, 20/20 in every mode). `docs/reindex_accuracy/{results.csv,tier1_results.md,reindex_ppl_delta.png,tier1_*.json}`.
5. Deviations vs the docs: FlashInfer sparse backend on Blackwell; bf16 latent cache (576 B) not fp8 656 B; GLM checkpoints are BF16 → official FP8 repos used; GLM-5.2 skip offset 3; GLM-5 has DSA (run); decode not run-to-run deterministic; fast analysis twins (verified equal) replaced the O(N·window) originals; GPQA gated (dropped); tiers 2/3 of exp3 deferred.
6. Budget: exp0 5, exp1 ≈18 (incl. 8 of determinism probes), exp2 ≈24, exp3 ≈17 (incl. unit tests) → ≈ 65 GPU-h; wall-clock 17.5 h. Nothing pushed; all commits carry both trailers; raw traces sharded ≤45 MB.
7. Open items for the user: (a) push, (b) tier 2/3 of exp3 (more documents/benchmarks, MMLU-Pro thinking; ~15–45 GPU-h per model), (c) GPQA access, (d) consumers of the numbers (ramulator `MEASURED_OVERLAP`/`MEASURED_TOP10`, paper §3.3) — the V3.2 values above replace the CPU V4 series.
