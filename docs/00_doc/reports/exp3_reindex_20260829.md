# exp3 re-index gate report (tier 1) — 2026-08-29 — verdict: PASS

1. Modes per model (same items, same engine): `ctrl_identity`, `ctrl_numeric` (batch-composition control), `perm_once_A` (64-token block permutation + block-table update), `perm_once_B` (per-row permutation, block table untouched = the re-index design; seeds 7/8/9 for V3.2 at 32K), all applied to the written prefix at the prefill-chunk boundary before the scored continuation; `perm_periodic_B` (every 4 decode steps, 10 % of the prefix swapped) in the RULER generation block. Hook `scripts/gpu/selhook/reindex_ext.py`; unit test `docs/reindex_accuracy/unit_test_v32.json`.
2. Equivalence rule (`exp3_tier1_results.py`): mean paired ΔPPL vs clean within the clean2 floor bound, and per-token |Δlogprob| p90 within 1.5× max(floor, ctrl_numeric). Result: **77 of 78 mode rows equivalent** (`results.csv` column `equivalent_to_noise_floor`); the single non-equivalent row is V3.2 `perm_once_B` seed 7 at 32K (ΔPPL +0.0062 [−0.0004, +0.0178]), driven by one document (+0.057) — seeds 8 and 9 on the same documents give −0.0009 [−0.0020, +0.0002] and +0.0001 [−0.0014, +0.0016], and clean reruns 3/4 show the same per-document spread (up to +0.0066), so it is a high-variance document, not an impl-B effect.
3. Headline ΔPPL vs clean (impl B, books): V3.2 −0.0009/+0.0014/−0.0012 at 32K(seed 8)/64K/128K vs floor −0.0008/+0.0015/−0.0031; GLM-5.2 −0.0073/+0.0107/−0.0005 vs floor −0.0058/+0.0130/−0.0052; GLM-5 −0.0049/+0.0041/−0.0006 vs floor −0.0081/+0.0070/−0.0040. Impl A: V3.2 −0.0015/+0.0003, GLM-5.2 −0.0073/+0.0107, GLM-5 −0.0046/+0.0032 (32K/64K). Identity controls track clean2 exactly; ctrl_numeric is of the same size as the perm modes. WikiText-2 (2K prefix): all modes within ±0.003 of clean for every model.
4. Token-level: per-token |Δlogprob| distributions of perm modes coincide with the identical-rerun floor at 32K–128K (V3.2 p90 0.012–0.014 vs 0.013–0.025; GLM 0.25–0.64 vs 0.26–0.66). At the 2K WikiText prefix, V3.2 impl B shows 3× the rerun floor's p90 (0.19 vs 0.06) but the batch-composition control shows 0.13 — summation-order noise, unbiased in mean.
5. RULER generation (5 niah + 5 qa at 32K and 128K, greedy 32–128 tokens): identical accuracy in every mode — V3.2 16/20, GLM-5.2 18/20, GLM-5 20/20 for clean, perm_once_B and perm_periodic_B (the same qa misses each time); 16–20 of 20 token streams bit-identical to clean, the rest diverging at steps 2–43 (the near-tie regime seen between identical runs).
6. Checklist (exp3 §7): identity vs clean = within floor (bit-identity impossible, see clean report); row read-back after an impl-B swap OK (both cache groups: MLA latent 576 B rows, indexer 132 B rows with the `[64×128 fp8][64×4 B scales]` block layout); perm_log replay covers [0, prefix−64) with no duplicates (permutations logged verbatim); no unit at or beyond seq_len−64 touched (assertions in the hook, margins ≥1 in the unit test); `results.csv` has one row per model × benchmark × prefix × mode; figure `reindex_ppl_delta.png`.
7. Deviations: KV cache dtype is vLLM's default bf16 latent (576 B/token/layer + 132 B indexer) not the 656-B fp8 layout in the docs; impl B needs the indexer-cache block layout (found via the CUDA kernel); PPL prefix re-indexing happens at a chunk boundary rather than "between decode steps" (the chunk boundary is the same point in the cache lifecycle: prefix written, next tokens not yet). Tiers 2/3 (larger n, more benchmarks, MMLU-Pro thinking) deferred per user.
