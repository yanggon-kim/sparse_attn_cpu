# exp2 gate report (GLM-5.2 + GLM-5 cross-model generality) — 2026-08-29 — verdict: PASS

1. Runs: same 135-prompt ladder as exp1 (rung = V3.2 token count; GLM counts ≈0.25 % lower, recorded per run), same hooks/scripts. GLM-5.2 FP8 (78 layers, 21 computing the top-k, `index_topk_freq` 4, offset 3): 135 runs + variant (b) re-adaptation (computing layers only), 9.6 GPU-h; GLM-5 FP8 (78 layers, no index sharing): 135 runs, 11.9 GPU-h; smoke 1.9 GPU-h. Run ids `glm52_<sample_id>[_b]`, `glm5_<sample_id>` in `docs/glm_sweep/R1_kv_locality.csv`. Campaign GPU-h ≈ 48.
2. R1 adjacent overlap at 8K/16K/32K/64K/128K — **DeepSeek-V3.2 0.843/0.790/0.751/0.741/0.723; GLM-5.2 (a, all 78 layers) 0.846/0.789/0.750/0.722/0.702; GLM-5.2 (b, 21 computing layers) 0.847/0.791/0.745/0.714/0.692; GLM-5 0.849/0.791/0.753/0.730/0.715**. Lift over random: V3.2 2.56/5.27/10.2/21.9/41.5×; GLM-5.2 2.55/5.25/10.1/21.3/40.4×; GLM-5 2.56/5.26/10.2/21.6/41.2×. Recency baselines 0.47–0.49 → 0.28–0.31. Long-decode retention (2048 steps) lag 512 / 1024: V3.2 0.58→0.47 / 0.43→0.34; GLM-5.2 0.56→0.36 / 0.44→0.29; GLM-5 0.57→0.41 / 0.44→0.30.
3. (a) vs (b) for GLM-5.2: Δ adjacent overlap +0.002/+0.002/−0.005/−0.008/−0.010 (b − a); A@99 within 0.4 pt; the shared layers reproduce their producer's set exactly (verified bit-for-bit in the smoke), so per-layer averaging makes the memory-system view (a) and the algorithm view (b) nearly identical — no inflation artefact.
4. R2 MoE (8 of 256, 75 learned layers for GLM): adjacent overlap GLM-5.2 0.28–0.29 (9.0–9.4×), GLM-5 0.29–0.31 (9.1–9.8×), V3.2 0.24–0.26 (7.7–8.3×); context-independent for all three.
5. R3 hot set A@99 (% of pool): V3.2 65.0/44.7/30.3/16.7/9.7; GLM-5.2 62.1/42.7/29.1/18.0/11.5; GLM-5 61.5/42.5/28.9/18.6/11.0. Top-10 % coverage (MEASURED_TOP10): 0.30/0.57/0.79–0.80/0.92–0.94/0.96–0.98 for all three.
6. Generality claim (paper §3.3): *across DeepSeek-V3.2, GLM-5.2 and GLM-5 — three DSA models from two labs with 61–78 layers and with/without index sharing — the per-step top-2048 gather overlaps 84–85 % at 8K and still 69–72 % at 128K (40× a random selector), and 99 % of gathers hit a hot set shrinking from ~62–65 % to ~10–12 % of the KV pool; the curves coincide within ±0.02 overlap / ±2 pt A@99.* Run ids per number in the CSVs.
7. Files: `docs/glm_sweep/{R1_kv_locality,R2_moe_locality,R3_hotset_coverage,accuracy_by_source}.csv` (combined) + per-model/variant CSVs, `sweep_glm52_glm52_{a,b}.json`, `sweep_glm5_glm5.json`, `glm_sweep_summary.md`, `side_by_side.png` (+ `glm_0{1..4}_*.png`), `runs/<run_id>/` (glm52 135 + glm5 135 dirs with sharded gz traces; `_b` dirs carry analysis only — their traces are a subset of the (a) run).
8. Verification (`verify_sweep.py`): PASS for glm52 (a) and glm5 (validate PASSED 135/135 each; CSV aggregates re-derived exactly; overlap/lift/recency/A@99 monotonic; retention monotonic in lag for every bf run ≥64 steps — shorter decodes and looping ld runs excluded from that check). RULER bf accuracy (n=8/rung): GLM-5.2 0.88/0.88/0.88/1.0/0.75, GLM-5 0.88/1.0/1.0/1.0/0.88 (needles 100 %, qa the usual 32-token-cap misses).
9. Deviations: KV cache dtype is vLLM's default `auto` (bf16 latent rows of 576×2 B) rather than the 656-B `fp8_ds_mla` layout assumed in the docs — irrelevant for selection statistics (the indexer's own fp8 key cache drives top-k); noted for exp3 byte accounting. GLM-5 has DSA and was run (user decision). Gate: PASS → exp3 boundary (stop conditions §5(b)/(c) to raise: determinism, budget, GPQA).
