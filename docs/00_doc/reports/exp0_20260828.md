# exp0 gate report — 2026-08-28 — verdict: PASS

1. Node: 8x NVIDIA B200 183 GB (1.47 TB HBM), SM 10.0, driver 595.91.07, CUDA 13.0 toolkit; vLLM 0.28.0 (`2cf0a69`, contains pin `5559679`), torch 2.13.0+cu130, flashinfer 0.6.16.post3 (+ prebuilt jit-cache cu130). `reports/node.json`.
2. Backend on this SM (VERIFY): **FLASHINFER_MLA_SPARSE** (kv `fp8_ds_mla`, HND), not FLASHMLA_SPARSE; MoE = FLASHINFER_TRTLLM fp8 *monolithic*. Hook patches both sparse impl classes; MoE ids captured via the kernel's routing-replay `set_capture_fn` (vLLM's own `enable_return_routed_experts` refuses MLA-sparse KV groups).
3. Smoke (exp0 §4): DeepSeek-V3.2 FP8, TP8, eager, prefix-caching off, seed 42, max_model_len 16384: "The capital of France is" → `[11111, 16, 455]` " Paris. The", identical ×2 (hook-off) and hook-on. Load 479 s cold / 33 s weights warm; 171 GB/GPU after load (75.4 GiB KV → 1.03 M tokens).
4. Hook unit checks (exp0 §5, `scripts/gpu/check_smoke.py`): 61×3 KV records, all `sel` in [0,pos], len(sel)=min(2048,n_comp), n_comp=pos+1, layers 0..60 once per step, 58×3 MoE records with 8 distinct ids in [0,256), hook-on == hook-off tokens → `smoke_identity.txt` = IDENTICAL. 16/16 PASS.
5. Adapter `scripts/gpu/vllm_to_ds4_run.py` → `ingest_trace.py` → `validate_trace.py` **PASSED** → `analyze_locality.py`, `ingest_moe_trace.py`, `analyze_moe_locality.py` all run on run `v32_smoke0_capital_s0` (58 MoE layers, adj overlap 0.297, lift 9.5x on 3 steps — smoke only).
6. Deviations recorded: (a) GLM checkpoints `zai-org/GLM-5.2`/`GLM-5` are BF16 (1.4 TB) → official `-FP8` repos used (indexer unquantized); (b) GLM-5.2 `index_skip_topk_offset`=3 (doc: 2) → computing layers {0,1,2,6,10,…,74} (21/78); (c) GLM-5 has `index_topk` → DSA on all 78 layers, will be run; (d) sparse builder treats short prefills as "decodes" (num_decode_tokens≠num_decodes) → hook derives rows from query_start_loc/seq_lens; (e) request ids get a `-<hex>` suffix inside the engine (adapter matches by prefix); (f) `analyze_locality.py` got a one-line guard for absent per-index scores (scores optional per exp1 §1); (g) no official RULER/LongBench-v2/InfiniteBench numbers exist for any target model (exp3 §5(b) per benchmark); GPQA gated access accepted by the user.
7. Files: `scripts/gpu/{smoke_load,make_node_json,check_smoke,vllm_to_ds4_run}.py`, `scripts/gpu/selhook/worker_ext.py`, `docs/00_doc/{PROGRESS.md,reports/node.json}`, `<WORKDIR>/env/activate.sh` (CUDA_HOME with lib64 symlink fixes flashinfer JIT link).
8. GPU-h: ≈5 of the 5 h exp0 split (four TP8 loads incl. two failed attempts: JIT link error, stale workers holding memory). Gate: PASS → continue to exp1 smoke.
