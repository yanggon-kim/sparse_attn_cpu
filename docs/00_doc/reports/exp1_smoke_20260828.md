# exp1 smoke gate report — 2026-08-28 — verdict: PASS

1. Run: DeepSeek-V3.2 FP8, TP8 eager, prefix caching off, seed 42; RULER `niah_single_2` 8K prompts s00–s03 (8047/7459/8051/8049 tokens with the V3.2 chat template, thinking off), forced 256 decode steps, batch of 4 (`max_num_seqs 4`) + s00 alone (`_solo`). Run ids `v32_ruler_niah_single_2_8192_s0{0..3}_smoke2`, `..._s00_solo_smoke2`; trace `trace_v32_8192_bf_smoke2` (152,320 records, 515 steps, 0 hook errors). All 5 needles retrieved. 0.45 GPU-h.
2. Unit checks (`check_smoke.py`, 15/15 PASS on s00): 61×256 KV records, sel ⊂ [0,pos], len(sel)=min(2048,n_comp), n_comp=pos+1, pos +1/step, layers 0..60 once/step; 58×256 MoE records with 8 distinct ids in [0,256).
3. Batch attribution (`compare_attribution.py`, PASS): each batched request's positions track its own prompt length; s00-in-batch vs s00-solo pre-divergence KV Jaccard 0.931 (random level 0.25), MoE 0.78 (random 0.03). **Deviation:** the doc expected bit-identical sets; batched vs solo decode is not batch-invariant on this stack (tokens diverge at step 10/256), and even two identical solo runs diverge (step 38) — fp8 TRTLLM MoE / FlashInfer sparse kernels are not run-to-run deterministic. Harmless for exp1/exp2 statistics; flagged for exp3 (a deterministic kernel config will be tested before exp3; vLLM `VLLM_BATCH_INVARIANT` does not cover sparse MLA / fp8 MoE).
4. Plausibility vs CPU V4 (s01, 256 steps, `analyze_locality_fast.py`): adjacent overlap **0.817** (CPU V4 8K: 0.790; expected 0.6–0.9), lift **3.02×** (CPU 2.92×), recency baseline 0.428 ≪ overlap, retention lag 8/64/128 = 0.73/0.64/0.58, hot-set A@99 = 67.1 % of pool (CPU 65.7 %), top-10 % coverage 0.35; MoE adjacent overlap 0.246, lift 7.9× (58 learned layers, no hash layers).
5. Chain: adapter → `ingest_trace_fast.py` (verified column-identical to `ingest_trace.py` on smoke0) → `validate_trace.py` PASS (all 5 runs) → `analyze_locality_fast.py` (vectorized twin; the original's reuse-distance loop is O(N·window) and still running after 30 min on 256 steps — equivalence check on these runs pending, recorded in the ladder report) → `extended_retention.py` → `ingest_moe_trace.py` → `analyze_moe_locality.py` → `analyze_hotset_coverage.py`.
6. Hook changes since exp0: hook point moved from `forward_mqa` to `MLAAttention.forward_impl` (long prefill chunks use the dense-MHA path and never reach `forward_mqa`, which lost decode step 0); rows derived from `query_start_loc`/`seq_lens` (the sparse builder counts short prefills as "decodes").
7. Ladder prompts (`build_gpu_prompts.py`): 135 samples — bf 20/20/20/19/16 at 8K/16K/32K/64K/128K (RULER niah+qa 8, LongBench v1 4 (≤32K), LongBench v2 4, InfiniteBench 4), ld 8 per rung (LongBench v1 summarization / InfiniteBench En.QA + En.Sum, 2048 forced tokens). Rung 8K (28 runs, 1.32 GPU-h) already generated; analysis running. GPU-h so far ≈ 7.5 (exp0 5 + smokes 1.4 + rung 8K 1.3).
8. Gate: PASS → ladder continues (16K launched).
