# exp1 ladder gate report — 2026-08-29 — verdict: PASS

1. Runs: DeepSeek-V3.2 FP8 (vLLM 0.28.0, TP8 eager, FLASHINFER_MLA_SPARSE, k=2048, 61 layers), 135 runs = 28/28/28/27/24 at 8K/16K/32K/64K/128K (bf: RULER niah+qa 8, LongBench v1 4 (≤32K), LongBench v2 4, InfiniteBench 4; ld: 8 per rung, 2048 forced steps). Run ids `v32_<sample_id>` in `docs/gpu_sweep/R1_kv_locality.csv`; batch logs `batch_v32_<rung>_bf_ld.json`. Generation 1.32/1.42/1.60/1.80/2.13 GPU-h; ladder total ≈ 8.3 GPU-h; campaign so far ≈ 23 GPU-h (exp0 5, smokes 1.4, ladder 8.3, determinism probes ~8).
2. R1 (mean over all runs of a rung, 95 % bootstrap CI over runs): adjacent overlap **0.843 / 0.790 / 0.751 / 0.741 / 0.723**, lift over random **2.56× / 5.27× / 10.2× / 21.9× / 41.5×**, recency baseline 0.49 / 0.39 / 0.32 / 0.30 / 0.31 (CPU V4-Flash k=512: 0.790 / 0.718 / 0.672 / 0.668 and 2.9 / 5.7 / 10.5 / 21.4× — V3.2 sits slightly above the V4 curve, same shape). Long-decode retention (2048 steps): lag 512 = 0.58 / 0.51 / 0.49 / 0.48 / 0.47, lag 1024 = 0.43 / 0.39 / 0.37 / 0.35 / 0.34 — keeps decaying, no plateau.
3. R2 (MoE, 58 learned layers, 8 of 256): adjacent overlap 0.254 / 0.258 / 0.242 / 0.253 / 0.254, lift 8.1× / 8.3× / 7.7× / 8.1× / 8.1× — context-independent (CPU V4: 0.33–0.38, 16×; V3.2 routes less persistently).
4. R3 (hot set): A@99 = **65.0 / 44.7 / 30.3 / 16.7 / 9.7 %** of the pool (pool 6.6K / 14K / 28K / 61K / 119K; CPU V4: 65.7 / 47.5 / 31.4 / 20.4 % at 8K–64K); top-10 % coverage (`MEASURED_TOP10`) = 0.304 / 0.565 / 0.788 / 0.935 / 0.978; `MEASURED_OVERLAP` = the R1 overlaps above.
5. Files committed: `docs/gpu_sweep/{R1_kv_locality,R2_moe_locality,R3_hotset_coverage,accuracy_by_source}.csv`, `sweep_v32.json`, `gpu_sweep_summary.md`, `gpu_0{1..4}_*.png`, and `docs/gpu_sweep/runs/<run_id>/` (135 dirs, 13 GB: req/meta/manifests, analysis JSON+parquet, `traces/*.jsonl.gz.partNN` ≤45 MB with `SHARDS.json` sha256s — user decision to commit raw data).
6. Verification (`verify_sweep.py`, PASS): validate_trace PASSED 135/135; CSV aggregates re-derived from the per-run JSONs (exact); overlap non-increasing and lift strictly increasing with context; recency < overlap at every rung; A@99 non-increasing; retention non-increasing in lag for every bf run; all 40 ld runs ≥ 2048 steps. Observation: one ld run (`v32_ib_longbook_qa_eng_65536_ld01`) collapsed into a period-32 repetition loop ("**Final Answer:** …"), which raises its lag-32 retention — real behaviour, kept in the data.
7. Analysis tooling: `ingest_trace_fast.py`, `analyze_locality_fast.py` (numba reuse distance), `extended_retention_fast.py` — each verified equal to the original script on smoke/512-step runs (0 differing columns); the originals are O(N·window) and did not finish on 2048-step runs. Memory-aware queue (`queue_analysis.sh`) after a 1.3 TB RAM peak. Deviations: 16 8K runs were initially skipped (chain started before the adapter finished writing) and re-queued; per-index scores not traced (optional per exp1 §1), so score-based columns are NaN.
8. Accuracy sanity (bf, our scorer, n=4–8 per cell): RULER niah 100 % at every rung, qa 25–75 % (32-token cap vs verbose chat-mode answers), passkey/number-string 100 % except one 16K passkey miss (truncation), En.MC 50–100 %, LongBench v2 25–50 %; ROUGE-L 0.13–0.24 for v1 summaries. Not comparable to official numbers (sub-samples, chat mode).
9. Gate: PASS → exp2 (GLM-5.2 smoke launched). Open items carried forward: run-to-run non-determinism (all 10 probed kernel/allreduce/cudagraph configurations diverge, `docs/reindex_accuracy/determinism_probes.md`) and GPQA gated access — both to be raised before exp3.
