# exp3 boundary — STOP for user decisions (GPU_CAMPAIGN.md §5 (b) and (c)) — 2026-08-29 08:45 UTC

State: exp0/exp1/exp2 gates PASS and committed (db02462, a7fee27). Campaign GPU-h ≈ 48 of 1,000 (exp0 5, exp1 ~18 incl. probes, exp2 ~24, unit tests ~1). Wall-clock 12 h. GPU idle.

1. §5(b) determinism (exp3 §7 item 1 "clean twice → identical token_ids"): NOT achievable on this stack. 11 probes (DeepSeek-V3.2, 8K prompt, 512 greedy steps ×3) — baseline, DeepGEMM / Triton MoE, NCCL-only allreduce, cudagraphs, FlashMLA-sparse attention, combinations — all diverge run-to-run at steps 10–24 (near-ties; top-1 logprob noise ≤0.08 nats); FlashInfer-CUTLASS MoE and VLLM_BATCH_INVARIANT are unsupported for sparse MLA. `docs/reindex_accuracy/determinism_probes.md`. Proposed method: replace "bit-identical" by "indistinguishable from the run-to-run noise floor": for every benchmark compare perm runs against clean with (i) paired accuracy delta + 95 % bootstrap CI, (ii) first-divergence-step / top-1 agreement / max|Δlogit| distributions vs clean-vs-clean2 (KS p > 0.05) and vs ctrl_numeric. The unit test already shows this pattern (all modes correct; divergence steps 10–72 for perm modes vs 20–24 for clean2).
2. §5(c) budget: the exp3 design as written (RULER 13×3×100 + LongBench-v2 503 + InfiniteBench 529 + MMLU-Pro 1,000 = 5,932 items ≈ 396 M prompt tokens) costs ≈145 GPU-h per mode per model at the measured ~7 K tok/s prefill; 7 modes × 3 models ≫ the 40 GPU-h split. Options (per model, all modes on the same items, seed 42 subsets, `subset_exp3.py`):
   - A "as designed": ≈1,000 GPU-h per model → not feasible.
   - B "lean" (recommended): subset 1,490 items (RULER 4 tasks × 3 lengths × 50, LB-v2 200 stratified, IB 200, MMLU-Pro 490 thinking) for clean / ctrl_numeric / perm_once B / perm_periodic B (the design), plus a 40-item agreement sample for clean2 / ctrl_identity / perm_once A / perm_periodic A ≈ 150 GPU-h per model → V3.2 only ≈150; all three ≈450 (fits the remaining ~950).
   - C "minimal": ~700 items (RULER 4×3×25, LB-v2 100, IB 100, MMLU-Pro 200) ≈ 70 GPU-h per model → all three ≈ 210.
3. §5(b) GPQA-Diamond: `Idavidrein/gpqa` still returns "Access denied — requires approval" for the yang-gon account (checked 2026-08-29). Without it the official-number gate rests on MMLU-Pro (V3.2 85.0, thinking mode; no MMLU-Pro number published for GLM-5/5.2) — say whether to keep waiting for access, use a mirror, or drop GPQA.
Also to confirm: hybrid thinking (MMLU-Pro/GPQA in thinking mode ≤8K tokens, long-context benchmarks non-thinking); KV cache dtype stays vLLM default `auto` (bf16 latent, 1152 B/token/layer + 132 B indexer) for exp3 as in exp1/2.
