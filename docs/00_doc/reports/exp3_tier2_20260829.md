# exp3 tier 2 report (DeepSeek-V3.2, PPL block complete; stopped by the user before the re-indexed generation block) — 2026-08-29 — verdict: PASS (PPL)

1. Scope run: V3.2 only, one engine load (`scripts/gpu/exp3_tier2.py`, resumable). PPL: 30 long books × {32K, 64K, 128K} × 10 modes (clean, clean2, ctrl_identity, ctrl_numeric, perm_once_A/B × seeds 7/8/9) = 900 rows; WikiText-2 93 windows and PTB 32 windows × 10 modes = 1,250 rows. 2,150 rows, 1,505 re-index events, 0 hook errors. Generation block: `clean` 325/400 items recorded, then stopped (user: PPL is sufficient). GLM-5.2 / GLM-5 tier 2 not started.
2. Verdict rule as tier 1 (`exp3_tier2_results.py`): 38 of 40 mode rows equivalent to the identical-rerun floor; all 30 long-book rows pass; the two flagged rows are PTB impl-B seeds 7/9 (+0.006/+0.008 on PPL 5.80, ≈1 SE, seed 8 −0.002, CIs include 0) — an under-powered mean test on 32 windows, not a re-index effect (see `docs/reindex_accuracy/tier2/README.md`).
3. Headline (30 docs, impl B seeds 7/8/9 vs floor): 32K +0.0010/−0.0013/−0.0013 vs −0.0013; 64K +0.0005/+0.0023/+0.0023 vs −0.0019; 128K −0.0015/−0.0034/−0.0002 vs −0.0018 (baseline PPL 1.3058 / 1.2678 / 1.3674). Impl A: −0.0001…+0.0012, +0.0009…+0.0018, −0.0028…−0.0005. Identity and batch control inside the same band. The tier-1 seed-7 outlier disappears at n = 30.
4. Token-level: per-token |Δlogprob| p90 identical across every mode at 32K–128K (0.048–0.051 / 0.063–0.067 / 0.089–0.094 nats); at 2K impl B equals the batch-composition control (0.20–0.24) while impl A equals the rerun floor (0.05–0.10).
5. Clean generation sanity (V3.2): niah_single_2, niah_multikey_2, vt all 25/25 at every length; qa_1 18/17/14; LongBench-v2 14/25. `vt` needed `max_new_tokens` 30 → 256 (prompt lacks RULER's answer prefix; 0/25 before the fix). No re-indexed generation rows exist for tier 2 (tier-1 RULER rows remain the generation evidence).
6. GPU-h: ≈28 (PPL 19 + partial ACC 9); campaign cumulative ≈93 of 1,000. Artifacts: `docs/reindex_accuracy/tier2/` (results, per-item, raw JSON 29 MB, permlogs 2 parts ≤40 MB, figure); logs in `docs/00_doc/data_gpu/logs/exp3_tier2_*`.
7. Remaining if resumed later (≈10 h wall/model incl. the 256-token vt decode): V3.2 ACC perm_once_B + perm_periodic_B (~5 h), GLM-5.2 and GLM-5 tier-2 PPL (~2.5 h each) + ACC. `exp3_tier2.py --resume` picks up exactly where the JSON stops.

## Addendum 2026-08-31 — V3.2 re-indexed generation (task "tier 2 generation"), partial

8. Ran `exp3_tier2.py --resume --skip-ppl` (02:35–05:54 UTC): completed the baseline (400/400, **329 correct**), `perm_once_B` (400/400, **331 correct, +2**) and `perm_periodic_B` (225/400, **210 vs 210 baseline on the same items, 0 flips**) before an **external SIGTERM at 05:54 UTC**; at 05:58 the node was taken over by another workload (four `vllm … Qwen2.5-72B-Instruct` API servers from `<NODE>/symbiosys_72b/`, 157–163 GB on GPUs 0–3, later all 8). Not my processes; left untouched.
9. Per task: niah_single_2, niah_multikey_2 and vt are 25/25 at 32K/64K/128K in every mode; qa_1 0.72/0.68/0.56 → 0.76/0.76/0.60 under `perm_once_B`; LongBench-v2 0.550 → 0.530. All 10 flips (6 gained, 4 lost) are on qa_1/LongBench-v2 near-ties; 0 hook errors, 20,183 re-index events.
10. Not done: the last 175 `perm_periodic_B` items, the `clean2` identical-rerun generation baseline (queued, never ran — it is the floor for the "identical token streams" column), and GLM-5.2 / GLM-5 tier-2 PPL. All resume with `exp3_tier2.py --resume` once 8 GPUs are free; TP=8 is required for comparability with the recorded baseline (TP=4 on the free GPUs 4–7 would change reduction order, and the GLM FP8 weights do not fit in 4 GPUs).
11. GPU-h this session ≈35; exp3 cumulative ≈128, campaign ≈128 of 1,000.
