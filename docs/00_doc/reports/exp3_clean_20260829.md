# exp3 clean gate report (tier 1, revised protocol) — 2026-08-29 — verdict: PASS (protocol changed, user-approved)

1. Precondition change (§5(b), approved by the user 2026-08-29): "clean ×2 → identical tokens" is unattainable on this stack (11 kernel/allreduce/cudagraph probes, `docs/reindex_accuracy/determinism_probes.md`). Replaced by an InfiniGen-style **teacher-forced perplexity** primary metric with the identical rerun as the noise floor; GPQA dropped (gated); MMLU-Pro/LongBench-v2/InfiniteBench accuracy deferred to tier 3 (budget: tier 1 ≈ 8 GPU-h per model).
2. Clean runs (`clean`, `clean2`, extra `clean3/4` for V3.2 at 32K), one engine load per model, `max_num_batched_tokens=2048`, greedy, prefix caching off: long-book PPL (10 InfiniteBench books ≥133K tokens, 2K scored after 32K/64K/128K prefix) and WikiText-2 (50 windows, 2K prefix / 1K scored). Files `docs/reindex_accuracy/tier1_{v32,glm52,glm5}.json`, `results.csv`.
3. Noise floor (clean2 − clean, paired ΔPPL 95 % CI over documents; per-token |Δlogprob| p90): DeepSeek-V3.2 WikiText 3.0006 → +0.0018 [−0.0000, +0.0039], p90 0.059; books 32K 1.0939 → −0.0008 [−0.0035, +0.0014] (p90 0.013), 64K 1.1357 → +0.0015 [−0.0016, +0.0051], 128K 1.0966 → −0.0031 [−0.0094, +0.0022]. GLM-5.2: WikiText 2.9835 → +0.0013 [−0.0062, +0.0088] (p90 0.35); books 1.8828/2.2146/2.5960 → −0.0058/+0.0130/−0.0052. GLM-5: WikiText 2.8630 → −0.0042; books 1.6099/1.8880/2.0919 → −0.0081/+0.0070/−0.0040 (p90 0.26–0.39). The GLM stack is ~10× noisier per token than V3.2's; ~1.5 % of V3.2 tokens shift >0.5 nats between identical runs (MoE routing flips at near-ties).
4. Official-number gate: not applicable in tier 1 (no published PPL for these models on these texts; MMLU-Pro/GPQA deferred/dropped) — §5(b) recorded. RULER sanity (niah_single_2 + qa_1, 5+5 at 32K and 128K): clean accuracy V3.2 0.80/0.80, GLM-5.2 0.90/0.90, GLM-5 1.00/1.00 (misses are qa items under the 32-token cap).
5. GPU-h: exp3 tier 1 15.1 (V32 5.0 incl. seed diagnostic, GLM-5.2 4.8 incl. a rerun of the accuracy block after a max_model_len crash, GLM-5 5.3) + hook unit tests ~1.5. Campaign total ≈ 65 GPU-h. Gate: PASS → re-index runs (same engine loads) reported in `exp3_reindex_20260829.md`.
