# exp2 smoke gate report (GLM-5.2) — 2026-08-29 — verdict: PASS

1. Run: GLM-5.2 FP8 (`zai-org/GLM-5.2-FP8`, 78 layers, `index_topk` 2048, `index_topk_freq` 4, `index_skip_topk_offset` 3), vLLM 0.28.0 TP8 eager, FLASHINFER_MLA_SPARSE + FLASHINFER_TRTLLM fp8 MoE; RULER niah 8K s00–s03 (8027/7438/8027/8025 tokens with GLM's chat template, thinking off) ×256 forced steps, batch of 4 + s00 solo. Run ids `glm52_ruler_niah_single_2_8192_s0{0..3}_smoke`(+`_b`), trace `trace_glm52_8192_bf_smoke` (195,840 records, 514 steps, 0 hook errors). All 5 needles retrieved. 1.92 GPU-h (751 s cold load).
2. exp2 §3 checks (`check_smoke.py`, PASS): computing-layer set from the hook == config skip rule = {0,1,2,6,10,…,74} (21 of 78); every shared layer's `sel` equals its producer's bit-for-bit (`shared_from_layer`); 78×256 KV records; 75×256 MoE records (layers 3–77) with 8 distinct ids in [0,256); ranges/counts/pos+1 all PASS. Attribution PASS (structural + pre-divergence Jaccard 0.887 KV / 0.767 MoE vs random 0.25 / 0.03).
3. Chain (both variants): adapter (a) all 78 layers and (b) `--computing-only` (21 layers as CSA) → `ingest_trace_fast` → `validate_trace` **PASSED 10/10** → fast analyzers → MoE chain. s00 (256 steps): (a) adj overlap 0.847, lift 3.37×, recency 0.39; (b) 0.849 / 3.38× / 0.40; MoE adj overlap 0.36 (lift ~11×; V3.2: 0.25). Plausible vs V3.2 8K (0.82–0.84).
4. Deviations: `index_skip_topk_offset` = 3 (doc: 2); GLM's tokenizer yields ~0.25 % fewer tokens than V3.2 for the same prompt (rung defined by the V3.2 count, GLM count in `context_length_actual_tokens`). Variant (a) vs (b) differ by ≈0.002 in adjacent overlap at 8K (per-layer means; the shared layers reproduce their producer's set exactly).
5. Gate: PASS → GLM-5.2 ladder launched (`run_ladder.sh`, both variants); GLM-5 follows. GPU-h so far ≈ 25.
